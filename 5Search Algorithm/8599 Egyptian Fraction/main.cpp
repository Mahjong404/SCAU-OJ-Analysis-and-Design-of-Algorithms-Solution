//8599 埃及分数问题
#include <iostream>
#include <cstring>
using namespace std;
typedef long long ll;

ll a, b;
ll ans[105], tmp[105];
int bestDepth;
bool found;

ll gcd(ll x, ll y) { return y ? gcd(y, x % y) : x; }

// IDA*: 搜索第d个分数，共depth个
void dfs(ll num, ll den, int d, int depth, ll lo) {
    if (d == depth) {
        if (num != 1) return;
        tmp[d] = den;
        // 比较是否更好
        if (!found || den < ans[depth]) {
            for (int i = 1; i <= depth; i++) ans[i] = tmp[i];
            found = true;
        }
        return;
    }
    // 分母下界: max(lo, ceil(den/num))
    ll lo2 = max(lo, den / num + 1);
    // 分母上界: 剩余depth-d个单位分数，每个至少1/lo2，总和不超过num/den
    // (depth-d+1) / hi <= num/den → hi <= (depth-d+1)*den/num 太复杂
    // 简化：hi <= den * (depth - d + 1) / num
    ll hi = den * (depth - d + 1) / num;
    if (hi > 1000000) hi = 1000000;

    for (ll k = lo2; k <= hi; k++) {
        // 剪枝：如果选k作为分母，剩余分数最多能给多大贡献
        // 剩余depth-d个分数，每个至少1/k，总和 (depth-d)/k < num/den - 1/k
        if (num * k >= den * (depth - d + 1)) continue;

        tmp[d] = k;
        ll nnum = num * k - den;
        ll nden = den * k;
        ll g = gcd(nnum, nden);
        dfs(nnum / g, nden / g, d + 1, depth, k + 1);
    }
}

int main() {
    cin >> a >> b;
    ll g = gcd(a, b);
    a /= g; b /= g;

    if (a == 1) { cout << b << '\n'; return 0; }

    found = false;
    for (int d = 2; d <= 100; d++) {
        dfs(a, b, 1, d, 2);
        if (found) {
            for (int i = 1; i <= d; i++)
                cout << ans[i] << (i < d ? ' ' : '\n');
            break;
        }
    }
    return 0;
}

/*
Description
所谓埃及分数，是指分子为1的分数。
任何一个真分数都可以表示为不同的埃及分数之和的形式。
如2/3 = 1/2 + 1/6，但不允许2/3 = 1/3 + 1/3，因为加数中有相同的。
然而，一个分数的表示方式并不唯一，我们定义：
1）加数少的比加数多的好；
2）加数个数相同的，最小的分数越大越好；（即最小的分数的分母越小越好）
3）如果最小的相同则比较次小的，以此类推。

如：分数19/45可以表示如下：
19/45 = 1/3 +1/12 +1/180
19/45 = 1/3 +1/15 +1/45
19/45 = 1/3 +1/18 +1/30
19/45 = 1/4 +1/6 +1/180
19/45 = 1/5 +1/6 +1/18

我们选最好的是最后一种，因为1/18比1/180，1/45，1/30和1/180都大。

你的编程任务：给定真分数，设计一个算法，找到用"最好埃及分数"表示真分数的表达式。

输入格式
仅一行，包括两个整数a和b，它们之间用空格分开，分别表示分数的分子和分母。

输出格式
用最好的埃及分数表示法来表示分数中的分母，从小到大依次输出。

输入样例
19 45

输出样例
5 6 18

提示
我认为此题是所有题中难度最大的题，请不愿麻烦的同学绕道而行，请无畏困难的同学勇敢冲上！

由于本OJ系统无法展示公式，因此在Hint这栏将以贴图方式展示算法思想：

任何一个真分数 a/b （a小于b）一定能表示成埃及分数之和，因此一定能搜索到解。

按a/b可拆分成的单位分数的个数进行循环搜索。
1）先将a/b分子分母消去因子，化为最简形式。
2）如果a=1则直接输出b。
3）否则从拆分成2个项开始尝试。如果2个单位分数无法拼凑成a/b，则试3个……依此
类推。那么搜索得到解一定会满足加数个数最少这个要求。
4）如果当加数个数等于n项，且当前正在试探第m项（m大于等于1，且小于等于n）的时
候，分母应从小至大开始试探。

此题难度有几点：
1， 我们倒是知道埃及分数的最小的分母为2，但可能的最大的分母似乎是无穷大，那搜
索就没法穷尽（何时结束？）
    但其实有隐约束条件使得最大的分母也是有范围的，这个范围的分析如后。

2， 这搜索的对象，可能不是一棵树，而是搜多棵树！所搜的第一棵树是2层的多叉
数（表示2个分数相加）；
    第一棵树没找到，就搜第二棵树的3层多叉数（表示3个分数相加），
    第二棵还没找到，就搜第三棵树的4层多叉数（表示4个分数相加），
    ……。
    这些树，只要在某棵树中搜索到符合要求的，就不再搜后续的树，将当前这棵树搜
完，后退出整个搜索过程（这样保证加数个数最少），然后输出结果。
    但如果没有找到（指一个符合要求的都没有），就继续下一棵树的搜索。

3， 为什么要将当前这棵树搜完呢？（不搜完也要能确定已找到"最好"的埃及分数，
适当的剪枝倒是可以加快，但这里不详述，以搜完来讲解算法。）
    比如当前加数个数为n，同样有n个加数，可能有多组组合的可能，如，19/45的三
个埃及分数相加：
         19/45 = 1/3 +1/12 +1/180
         19/45 = 1/3 +1/15 +1/45
         19/45 = 1/3 +1/18 +1/30
         19/45 = 1/4 +1/6 +1/180
         19/45 = 1/5 +1/6 +1/18
    其中这种：19/45 = 1/3 +1/12 +1/180会最先搜索到，但它并不是"最好"的埃及
分数表示，还得继续搜，直至把三个加数的三层多叉树搜完为止。搜完了，才知道这些
都满足埃及分数的表达中，最后一个分式的分母哪个是最小的，才能输出它。


---------------------------------------
现在分析前述本题难点的第一点： 搜索过程中，由隐约束条件来计算每个当前分数分母
的上下界（尤其是上界，分母可以大到多少？）：

先定义符号：
n为需要的单位分数个数，n从2个开始尝试，不成功而后3个……
m表示当前正在搜索第m个分数（m从1到n）
egp[m]存储当前搜索得到的第m个分数的分母
a/b为题目输入的真分数

<left><img src="pic/8599_1383871655704_62621.jpg"></left>
<left><img src="pic/8599_1383871670748_39999.jpg"></left>
<left><img src="pic/8599_1383871686000_14114.jpg"></left>
*/
